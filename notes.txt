Task 3
    1. [Task 3] Changed Build Process
        * added `bash` to run `build.sh`
        * fixed `android` -> `Android`
        * fixed `appName = "Test App"` -> `appName="Test App"`
        * fixed `${$appName}` -> `"$appName"`
    2. [Task 3] Changed Upload Process
        * added `bash` to run `upload.sh`
        * added `platform` parameter
        * added command to loop to fetch all artifacts within the output build directory then upload them
        * fixed broken upload process command
    3. [Task 3] Added Upload Process UPLOAD_TOKEN
        * fixed missing `UPLOAD_TOKEN` environment variable
    4. additional notes:
        A. I don't like that the platform for Build/Upload is not sanitized, maybe we can fix this later
Task 4
    1. [Task 4] Adding App Custom Name
        * added `project_name` to the `workflow_dispatch` inputs
        * changed `appName="Test App"` to use `${{github.event.inputs.project_name}` to reference `workflow_dispatch` input
        * changed `appName` to `project_name` to better match `build.sh`'s API
    2. additional notes:
        A. there's a build warning, maybe this is to be fixed in Task 8
        ```
        Node.js 20 actions are deprecated. The following actions are running on Node.js 20 and may not work as expected: actions/checkout@v2. Actions will be forced to run with Node.js 24 by default starting June 2nd, 2026. Please check if updated versions of these actions are available that support Node.js 24. To opt into Node.js 24 now, set the FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true environment variable on the runner or in your workflow file. Once Node.js 24 becomes the default, you can temporarily opt out by setting ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION=true. For more information see: https://github.blog/changelog/2025-09-19-deprecation-of-node-20-on-github-actions-runners/
        ```
Task 5
    1. Summary of Task:
        A. needs versioning input param
        B. only upload if push to a production branch 
        C. how versioning? semantic versioning system? incremental?
        D. when do we run build?
    2. [Task 5] Versioning & Custom Build and Upload
        * added `project_version_base` to the `workflow_dispatch` inputs
        * added workflow trigger on `push` event to a production branch
        * added new workflow step to prepare build metadata given inputs
        * added versioning as an input parameter to the Build step
        * added GitHub conditional on the Upload step if the "push to production" event occurs 
    3. [Task 5] Adding Dev Artifacts Upload
        * added upload for development-only artifacts using GitHub's upload-artifact action
        * changed input descriptions
        * fixed error with project_name
    4. [Task 5] Fixed Dev Artifacts Upload
        * fixed step order, dev artifacts upload ran before build
        * fixed typo with `meta.outputs.version`
    5. additional notes:
        A. Might be nice to add semantic versioning
        B. Maybe check to see if no files are found after collecting artifacts for dev upload; failing loudly if a future change breaks build output
        C. ^ never mind, I added the check!
Task 6
    1. [Task 6] Added Build Profiling Support
        * added `profile_build` to the `workflow_dispatch` inputs
        * added `profile_build` field to `steps.meta.outputs`
        * added conditional use of the `--profile` argument defined within `build.sh`
        * added error check to dev artifacts uploads for if no files are found
        * changed names of existing steps slightly
    2. [Task 6] Fixed Profiling Output
        * changed non-profiling output to exclude profiling files
        * fixed version naming error on profiling output
Task 7
    1. [Task 7] Added Matrix Build 
        * added iOS build support
        * added support for future build options
        * added matrix parameters for each supported target platform
        * added environment key to prevent a matrix job from running
        * removed hardcoded Android support
    2. [Task 7] Fixed Matrix Build Conditional
        * fixed matrix build conditional `or` that should have been `and`
Task 8
    1. [Task 8] Added Dev Artifacts Retention
        * changed upload for dev artifacts to have a month retention timeline
        * changed upload for dev profiling artifacts to have a week retention timeline
    2. [Task 8] Reduced Matrix Include Child Keys
        * changed path references to be uniform across platforms
        * changed syntax for existing expressions to make them more readable
        * removed paths within `Matrix Include` child keys